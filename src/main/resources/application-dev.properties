# Development Environment Configuration

# Development Log Levels
logging.level.com.smallnine.apiserver=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.apache.ibatis=DEBUG
logging.level.org.springframework.security=DEBUG

# Development Database Configuration (if needed)
# spring.datasource.url=jdbc:postgresql://localhost:5432/bark_bijou_dev

# Development Server Settings
server.port=8080
management.endpoints.web.exposure.include=*

# Development JWT Configuration (shorter expiration for testing)
# jwt.expiration=3600000

# Development Debug Features
debug=false
trace=false