<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smallnine.apiserver.dao.EmailVerificationDao">

    <resultMap id="EmailVerificationResultMap" type="com.smallnine.apiserver.entity.EmailVerification">
        <id property="id" column="id" />
        <result property="email" column="email" />
        <result property="otpToken" column="otp_token" />
        <result property="secret" column="secret" />
        <result property="expiresAt" column="expires_at" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO email_verification (email, otp_token, secret, expires_at, created_at)
        VALUES (#{email}, #{otpToken}, #{secret}, #{expiresAt}, NOW())
    </insert>

    <select id="findByEmailAndOtp" resultMap="EmailVerificationResultMap">
        SELECT * FROM email_verification
        WHERE email = #{email} AND otp_token = #{otpToken}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="findBySecret" resultMap="EmailVerificationResultMap">
        SELECT * FROM email_verification
        WHERE secret = #{secret}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="findBySecretAndOtp" resultMap="EmailVerificationResultMap">
        SELECT * FROM email_verification
        WHERE secret = #{secret} AND otp_token = #{otpToken}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM email_verification WHERE email = #{email}
    </select>

    <delete id="deleteByEmail">
        DELETE FROM email_verification WHERE email = #{email}
    </delete>

</mapper>
