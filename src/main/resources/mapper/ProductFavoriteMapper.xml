<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smallnine.apiserver.dao.ProductFavoriteDao">

    <resultMap id="FavoriteResultMap" type="com.smallnine.apiserver.entity.Favorite">
        <id property="id" column="id" />
        <result property="memberId" column="member_id" />
        <result property="productId" column="product_id" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="findByMemberId" resultMap="FavoriteResultMap">
        SELECT * FROM product_favorites
        WHERE member_id = #{memberId}
        ORDER BY created_at DESC
    </select>

    <select id="existsByMemberIdAndProductId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM product_favorites
        WHERE member_id = #{memberId} AND product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_favorites (member_id, product_id, created_at)
        VALUES (#{memberId}, #{productId}, NOW())
    </insert>

    <delete id="deleteByMemberIdAndProductId">
        DELETE FROM product_favorites
        WHERE member_id = #{memberId} AND product_id = #{productId}
    </delete>

</mapper>
